START: 1001;
BUCHI: {68002};
CUTPOINT: {68001, 1001, 1003, 46001, 68002, 68003, 46003};
PRE: {xtmp>=0 && xtmp<=64 && tmp>=0 && tmp<=64 && x>=0 && x<=64 && y>=0 && y<=64 };


FROM: 1001;
assume(-1 * x>=0 && -1 * 1  +  -1 * y>=0);
assume(tmp>=0);
tmp := y;
xtmp := x;
x := y;
y := x;
TO: 1001;

FROM: 1001;
assume(-1 * x>=0 && -1 * 1  +  -1 * y>=0);
assume(tmp<0);
tmp := y;
xtmp := x;
x := y;
y := x;
TO: 1003;

FROM: 1003;
assume(-1 * x>=0 && -1 * 1  +  -1 * y>=0);
tmp := y;
xtmp := x;
x := y;
y := x;
TO: 1003;

FROM: 1001;
assume(-1 * y>=0 && -1 * 1  +  -1 * y>=0);
assume(tmp>=0);
tmp := y;
xtmp := x;
x := y;
y := x;
TO: 1001;

FROM: 1001;
assume(-1 * y>=0 && -1 * 1  +  -1 * y>=0);
assume(tmp<0);
tmp := y;
xtmp := x;
x := y;
y := x;
TO: 1003;

FROM: 1003;
assume(-1 * y>=0 && -1 * 1  +  -1 * y>=0);
tmp := y;
xtmp := x;
x := y;
y := x;
TO: 1003;

FROM: 1001;
assume(-1 * x  +  y>=0 && x  +  -1 * y>=0 && -1 * 1  +  x>=0 && -1 * 1  +  y>=0 && -1 * 1  +  -1 * y>=0);
assume(tmp>=0);
tmp := y;
xtmp := x;
x := y;
y := x;
TO: 1001;

FROM: 1001;
assume(-1 * x  +  y>=0 && x  +  -1 * y>=0 && -1 * 1  +  x>=0 && -1 * 1  +  y>=0 && -1 * 1  +  -1 * y>=0);
assume(tmp<0);
tmp := y;
xtmp := x;
x := y;
y := x;
TO: 1003;

FROM: 1003;
assume(-1 * x  +  y>=0 && x  +  -1 * y>=0 && -1 * 1  +  x>=0 && -1 * 1  +  y>=0 && -1 * 1  +  -1 * y>=0);
tmp := y;
xtmp := x;
x := y;
y := x;
TO: 1003;

FROM: 1001;
assume(-1 * 1  +  x  +  -1 * y>=0 && -1 * 1  +  x>=0 && -1 * 1  +  y>=0 && -1 * 1  +  -1 * y>=0);
assume(tmp>=0);
tmp := y;
xtmp := x;
x := x;
y := y;
TO: 46001;

FROM: 1001;
assume(-1 * 1  +  x  +  -1 * y>=0 && -1 * 1  +  x>=0 && -1 * 1  +  y>=0 && -1 * 1  +  -1 * y>=0);
assume(tmp<0);
tmp := y;
xtmp := x;
x := x;
y := y;
TO: 46003;

FROM: 1003;
assume(-1 * 1  +  x  +  -1 * y>=0 && -1 * 1  +  x>=0 && -1 * 1  +  y>=0 && -1 * 1  +  -1 * y>=0);
tmp := y;
xtmp := x;
x := x;
y := y;
TO: 46003;

FROM: 1001;
assume(-1 * 1  +  -1 * x  +  y>=0 && -1 * 1  +  x>=0 && -1 * 1  +  y>=0 && -1 * 1  +  -1 * y>=0);
assume(tmp>=0);
tmp := y;
xtmp := x;
x := x;
y := y;
TO: 46001;

FROM: 1001;
assume(-1 * 1  +  -1 * x  +  y>=0 && -1 * 1  +  x>=0 && -1 * 1  +  y>=0 && -1 * 1  +  -1 * y>=0);
assume(tmp<0);
tmp := y;
xtmp := x;
x := x;
y := y;
TO: 46003;

FROM: 1003;
assume(-1 * 1  +  -1 * x  +  y>=0 && -1 * 1  +  x>=0 && -1 * 1  +  y>=0 && -1 * 1  +  -1 * y>=0);
tmp := y;
xtmp := x;
x := x;
y := y;
TO: 46003;

FROM: 1001;
assume(-1 * x>=0 && -1 * 1  +  y>=0);
assume(tmp>=0);
tmp := y;
xtmp := x;
x := y;
y := x;
TO: 1001;

FROM: 1001;
assume(-1 * x>=0 && -1 * 1  +  y>=0);
assume(tmp<0);
tmp := y;
xtmp := x;
x := y;
y := x;
TO: 1003;

FROM: 1003;
assume(-1 * x>=0 && -1 * 1  +  y>=0);
tmp := y;
xtmp := x;
x := y;
y := x;
TO: 1003;

FROM: 1001;
assume(-1 * y>=0 && -1 * 1  +  y>=0);
assume(tmp>=0);
tmp := y;
xtmp := x;
x := y;
y := x;
TO: 1001;

FROM: 1001;
assume(-1 * y>=0 && -1 * 1  +  y>=0);
assume(tmp<0);
tmp := y;
xtmp := x;
x := y;
y := x;
TO: 1003;

FROM: 1003;
assume(-1 * y>=0 && -1 * 1  +  y>=0);
tmp := y;
xtmp := x;
x := y;
y := x;
TO: 1003;

FROM: 1001;
assume(-1 * x  +  y>=0 && x  +  -1 * y>=0 && -1 * 1  +  x>=0 && -1 * 1  +  y>=0);
assume(tmp>=0);
tmp := y;
xtmp := x;
x := y;
y := x;
TO: 1001;

FROM: 1001;
assume(-1 * x  +  y>=0 && x  +  -1 * y>=0 && -1 * 1  +  x>=0 && -1 * 1  +  y>=0);
assume(tmp<0);
tmp := y;
xtmp := x;
x := y;
y := x;
TO: 1003;

FROM: 1003;
assume(-1 * x  +  y>=0 && x  +  -1 * y>=0 && -1 * 1  +  x>=0 && -1 * 1  +  y>=0);
tmp := y;
xtmp := x;
x := y;
y := x;
TO: 1003;

FROM: 1001;
assume(-1 * 1  +  x  +  -1 * y>=0 && -1 * 1  +  x>=0 && -1 * 1  +  y>=0);
assume(tmp>=0);
tmp := y;
xtmp := x;
x := x;
y := y;
TO: 46001;

FROM: 1001;
assume(-1 * 1  +  x  +  -1 * y>=0 && -1 * 1  +  x>=0 && -1 * 1  +  y>=0);
assume(tmp<0);
tmp := y;
xtmp := x;
x := x;
y := y;
TO: 46003;

FROM: 1003;
assume(-1 * 1  +  x  +  -1 * y>=0 && -1 * 1  +  x>=0 && -1 * 1  +  y>=0);
tmp := y;
xtmp := x;
x := x;
y := y;
TO: 46003;

FROM: 1001;
assume(-1 * 1  +  -1 * x  +  y>=0 && -1 * 1  +  x>=0 && -1 * 1  +  y>=0);
assume(tmp>=0);
tmp := y;
xtmp := x;
x := x;
y := y;
TO: 46001;

FROM: 1001;
assume(-1 * 1  +  -1 * x  +  y>=0 && -1 * 1  +  x>=0 && -1 * 1  +  y>=0);
assume(tmp<0);
tmp := y;
xtmp := x;
x := x;
y := y;
TO: 46003;

FROM: 1003;
assume(-1 * 1  +  -1 * x  +  y>=0 && -1 * 1  +  x>=0 && -1 * 1  +  y>=0);
tmp := y;
xtmp := x;
x := x;
y := y;
TO: 46003;

FROM: 1001;
assume(y>=0 && -1 * y>=0);
assume(tmp>=0);
tmp := tmp;
xtmp := xtmp;
x := x;
y := y;
TO: 68001;

FROM: 1001;
assume(y>=0 && -1 * y>=0);
assume(tmp<0);
tmp := tmp;
xtmp := xtmp;
x := x;
y := y;
TO: 68003;

FROM: 1003;
assume(y>=0 && -1 * y>=0);
tmp := tmp;
xtmp := xtmp;
x := x;
y := y;
TO: 68003;

FROM: 46001;
assume(-1 * 1  +  xtmp  +  -1 * y>=0);
assume(tmp>=0);
tmp := tmp;
xtmp := xtmp  +  -1 * y;
x := x;
y := y;
TO: 46001;

FROM: 46001;
assume(-1 * 1  +  xtmp  +  -1 * y>=0);
assume(tmp<0);
tmp := tmp;
xtmp := xtmp  +  -1 * y;
x := x;
y := y;
TO: 46003;

FROM: 46003;
assume(-1 * 1  +  xtmp  +  -1 * y>=0);
tmp := tmp;
xtmp := xtmp  +  -1 * y;
x := x;
y := y;
TO: 46003;

FROM: 46001;
assume(-1 * xtmp  +  y>=0);
assume(tmp>=0);
tmp := tmp;
xtmp := xtmp;
x := tmp;
y := xtmp;
TO: 1001;

FROM: 46001;
assume(-1 * xtmp  +  y>=0);
assume(tmp<0);
tmp := tmp;
xtmp := xtmp;
x := tmp;
y := xtmp;
TO: 1003;

FROM: 46003;
assume(-1 * xtmp  +  y>=0);
tmp := tmp;
xtmp := xtmp;
x := tmp;
y := xtmp;
TO: 1003;

FROM: 68001;
assume(tmp>=0);
TO: 68002;

FROM: 68002;
assume(tmp>=0);
TO: 68002;

FROM: 68001;
assume(tmp<0);
TO: 68003;

FROM: 68002;
assume(tmp<0);
TO: 68003;

FROM: 68003;
TO: 68003;
