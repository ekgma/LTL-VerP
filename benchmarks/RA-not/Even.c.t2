START: 1001;
BUCHI: {1004, 1002, 21004, 21002};
CUTPOINT: {1004, 21002, 1001, 21003, 21005, 1002, 21004, 1003};
PRE: {i>=0 && i<=64 };


FROM: 1001;
assume(-1 * i>=0 && -1 * 1  +  -1 * i>=0);
assume(i>=0);
i := -2 * 1  +  i;
TO: 1002;

FROM: 1001;
assume(-1 * i>=0 && -1 * 1  +  -1 * i>=0);
assume(i>=0);
i := -2 * 1  +  i;
TO: 1003;

FROM: 1001;
assume(-1 * i>=0 && -1 * 1  +  -1 * i>=0);
assume(i<0);
i := -2 * 1  +  i;
TO: 1004;

FROM: 1002;
assume(-1 * i>=0 && -1 * 1  +  -1 * i>=0);
i := -2 * 1  +  i;
TO: 1002;

FROM: 1003;
assume(-1 * i>=0 && -1 * 1  +  -1 * i>=0);
assume(i>=0);
i := -2 * 1  +  i;
TO: 1003;

FROM: 1003;
assume(-1 * i>=0 && -1 * 1  +  -1 * i>=0);
assume(i<0);
i := -2 * 1  +  i;
TO: 1004;

FROM: 1004;
assume(-1 * i>=0 && -1 * 1  +  -1 * i>=0);
i := -2 * 1  +  i;
TO: 1004;

FROM: 1001;
assume(-1 * i>=0 && -1 * 1  +  i>=0);
assume(i>=0);
i := -2 * 1  +  i;
TO: 1002;

FROM: 1001;
assume(-1 * i>=0 && -1 * 1  +  i>=0);
assume(i>=0);
i := -2 * 1  +  i;
TO: 1003;

FROM: 1001;
assume(-1 * i>=0 && -1 * 1  +  i>=0);
assume(i<0);
i := -2 * 1  +  i;
TO: 1004;

FROM: 1002;
assume(-1 * i>=0 && -1 * 1  +  i>=0);
i := -2 * 1  +  i;
TO: 1002;

FROM: 1003;
assume(-1 * i>=0 && -1 * 1  +  i>=0);
assume(i>=0);
i := -2 * 1  +  i;
TO: 1003;

FROM: 1003;
assume(-1 * i>=0 && -1 * 1  +  i>=0);
assume(i<0);
i := -2 * 1  +  i;
TO: 1004;

FROM: 1004;
assume(-1 * i>=0 && -1 * 1  +  i>=0);
i := -2 * 1  +  i;
TO: 1004;

FROM: 1001;
assume(-2 * 1  +  i>=0 && -1 * 1  +  -1 * i>=0);
assume(i>=0);
i := -2 * 1  +  i;
TO: 1002;

FROM: 1001;
assume(-2 * 1  +  i>=0 && -1 * 1  +  -1 * i>=0);
assume(i>=0);
i := -2 * 1  +  i;
TO: 1003;

FROM: 1001;
assume(-2 * 1  +  i>=0 && -1 * 1  +  -1 * i>=0);
assume(i<0);
i := -2 * 1  +  i;
TO: 1004;

FROM: 1002;
assume(-2 * 1  +  i>=0 && -1 * 1  +  -1 * i>=0);
i := -2 * 1  +  i;
TO: 1002;

FROM: 1003;
assume(-2 * 1  +  i>=0 && -1 * 1  +  -1 * i>=0);
assume(i>=0);
i := -2 * 1  +  i;
TO: 1003;

FROM: 1003;
assume(-2 * 1  +  i>=0 && -1 * 1  +  -1 * i>=0);
assume(i<0);
i := -2 * 1  +  i;
TO: 1004;

FROM: 1004;
assume(-2 * 1  +  i>=0 && -1 * 1  +  -1 * i>=0);
i := -2 * 1  +  i;
TO: 1004;

FROM: 1001;
assume(-2 * 1  +  i>=0 && -1 * 1  +  i>=0);
assume(i>=0);
i := -2 * 1  +  i;
TO: 1002;

FROM: 1001;
assume(-2 * 1  +  i>=0 && -1 * 1  +  i>=0);
assume(i>=0);
i := -2 * 1  +  i;
TO: 1003;

FROM: 1001;
assume(-2 * 1  +  i>=0 && -1 * 1  +  i>=0);
assume(i<0);
i := -2 * 1  +  i;
TO: 1004;

FROM: 1002;
assume(-2 * 1  +  i>=0 && -1 * 1  +  i>=0);
i := -2 * 1  +  i;
TO: 1002;

FROM: 1003;
assume(-2 * 1  +  i>=0 && -1 * 1  +  i>=0);
assume(i>=0);
i := -2 * 1  +  i;
TO: 1003;

FROM: 1003;
assume(-2 * 1  +  i>=0 && -1 * 1  +  i>=0);
assume(i<0);
i := -2 * 1  +  i;
TO: 1004;

FROM: 1004;
assume(-2 * 1  +  i>=0 && -1 * 1  +  i>=0);
i := -2 * 1  +  i;
TO: 1004;

FROM: 1001;
assume(-1 * 1  +  i>=0 && 1  +  -1 * i>=0);
assume(i>=0);
i := i;
TO: 21002;

FROM: 1001;
assume(-1 * 1  +  i>=0 && 1  +  -1 * i>=0);
assume(i>=0);
i := i;
TO: 21003;

FROM: 1001;
assume(-1 * 1  +  i>=0 && 1  +  -1 * i>=0);
assume(i<0);
i := i;
TO: 21004;

FROM: 1002;
assume(-1 * 1  +  i>=0 && 1  +  -1 * i>=0);
i := i;
TO: 21002;

FROM: 1003;
assume(-1 * 1  +  i>=0 && 1  +  -1 * i>=0);
assume(i>=0);
i := i;
TO: 21003;

FROM: 1003;
assume(-1 * 1  +  i>=0 && 1  +  -1 * i>=0);
assume(i<0);
i := i;
TO: 21004;

FROM: 1004;
assume(-1 * 1  +  i>=0 && 1  +  -1 * i>=0);
i := i;
TO: 21004;

FROM: 1001;
assume(i>=0 && -1 * 1  +  i>=0 && 1  +  -1 * i>=0);
assume(i>=0);
i := i;
TO: 21002;

FROM: 1001;
assume(i>=0 && -1 * 1  +  i>=0 && 1  +  -1 * i>=0);
assume(i>=0);
i := i;
TO: 21003;

FROM: 1001;
assume(i>=0 && -1 * 1  +  i>=0 && 1  +  -1 * i>=0);
assume(i<0);
i := i;
TO: 21004;

FROM: 1002;
assume(i>=0 && -1 * 1  +  i>=0 && 1  +  -1 * i>=0);
i := i;
TO: 21002;

FROM: 1003;
assume(i>=0 && -1 * 1  +  i>=0 && 1  +  -1 * i>=0);
assume(i>=0);
i := i;
TO: 21003;

FROM: 1003;
assume(i>=0 && -1 * 1  +  i>=0 && 1  +  -1 * i>=0);
assume(i<0);
i := i;
TO: 21004;

FROM: 1004;
assume(i>=0 && -1 * 1  +  i>=0 && 1  +  -1 * i>=0);
i := i;
TO: 21004;

FROM: 1001;
assume(-1 * 1  +  i>=0 && -1 * i>=0 && 1  +  -1 * i>=0);
assume(i>=0);
i := i;
TO: 21002;

FROM: 1001;
assume(-1 * 1  +  i>=0 && -1 * i>=0 && 1  +  -1 * i>=0);
assume(i>=0);
i := i;
TO: 21003;

FROM: 1001;
assume(-1 * 1  +  i>=0 && -1 * i>=0 && 1  +  -1 * i>=0);
assume(i<0);
i := i;
TO: 21004;

FROM: 1002;
assume(-1 * 1  +  i>=0 && -1 * i>=0 && 1  +  -1 * i>=0);
i := i;
TO: 21002;

FROM: 1003;
assume(-1 * 1  +  i>=0 && -1 * i>=0 && 1  +  -1 * i>=0);
assume(i>=0);
i := i;
TO: 21003;

FROM: 1003;
assume(-1 * 1  +  i>=0 && -1 * i>=0 && 1  +  -1 * i>=0);
assume(i<0);
i := i;
TO: 21004;

FROM: 1004;
assume(-1 * 1  +  i>=0 && -1 * i>=0 && 1  +  -1 * i>=0);
i := i;
TO: 21004;

FROM: 1001;
assume(i>=0 && -1 * i>=0 && 1  +  -1 * i>=0);
assume(i>=0);
i := i;
TO: 21002;

FROM: 1001;
assume(i>=0 && -1 * i>=0 && 1  +  -1 * i>=0);
assume(i>=0);
i := i;
TO: 21003;

FROM: 1001;
assume(i>=0 && -1 * i>=0 && 1  +  -1 * i>=0);
assume(i<0);
i := i;
TO: 21004;

FROM: 1002;
assume(i>=0 && -1 * i>=0 && 1  +  -1 * i>=0);
i := i;
TO: 21002;

FROM: 1003;
assume(i>=0 && -1 * i>=0 && 1  +  -1 * i>=0);
assume(i>=0);
i := i;
TO: 21003;

FROM: 1003;
assume(i>=0 && -1 * i>=0 && 1  +  -1 * i>=0);
assume(i<0);
i := i;
TO: 21004;

FROM: 1004;
assume(i>=0 && -1 * i>=0 && 1  +  -1 * i>=0);
i := i;
TO: 21004;

FROM: 1001;
assume(-1 * 1  +  i>=0 && i>=0 && 1  +  -1 * i>=0);
assume(i>=0);
i := i;
TO: 21002;

FROM: 1001;
assume(-1 * 1  +  i>=0 && i>=0 && 1  +  -1 * i>=0);
assume(i>=0);
i := i;
TO: 21003;

FROM: 1001;
assume(-1 * 1  +  i>=0 && i>=0 && 1  +  -1 * i>=0);
assume(i<0);
i := i;
TO: 21004;

FROM: 1002;
assume(-1 * 1  +  i>=0 && i>=0 && 1  +  -1 * i>=0);
i := i;
TO: 21002;

FROM: 1003;
assume(-1 * 1  +  i>=0 && i>=0 && 1  +  -1 * i>=0);
assume(i>=0);
i := i;
TO: 21003;

FROM: 1003;
assume(-1 * 1  +  i>=0 && i>=0 && 1  +  -1 * i>=0);
assume(i<0);
i := i;
TO: 21004;

FROM: 1004;
assume(-1 * 1  +  i>=0 && i>=0 && 1  +  -1 * i>=0);
i := i;
TO: 21004;

FROM: 1001;
assume(-1 * 1  +  i>=0 && -1 * i>=0 && i>=0 && 1  +  -1 * i>=0);
assume(i>=0);
i := i;
TO: 21002;

FROM: 1001;
assume(-1 * 1  +  i>=0 && -1 * i>=0 && i>=0 && 1  +  -1 * i>=0);
assume(i>=0);
i := i;
TO: 21003;

FROM: 1001;
assume(-1 * 1  +  i>=0 && -1 * i>=0 && i>=0 && 1  +  -1 * i>=0);
assume(i<0);
i := i;
TO: 21004;

FROM: 1002;
assume(-1 * 1  +  i>=0 && -1 * i>=0 && i>=0 && 1  +  -1 * i>=0);
i := i;
TO: 21002;

FROM: 1003;
assume(-1 * 1  +  i>=0 && -1 * i>=0 && i>=0 && 1  +  -1 * i>=0);
assume(i>=0);
i := i;
TO: 21003;

FROM: 1003;
assume(-1 * 1  +  i>=0 && -1 * i>=0 && i>=0 && 1  +  -1 * i>=0);
assume(i<0);
i := i;
TO: 21004;

FROM: 1004;
assume(-1 * 1  +  i>=0 && -1 * i>=0 && i>=0 && 1  +  -1 * i>=0);
i := i;
TO: 21004;

FROM: 1001;
assume(-1 * 1  +  i>=0 && 1  +  -1 * i>=0 && -1 * i>=0);
assume(i>=0);
i := i;
TO: 21002;

FROM: 1001;
assume(-1 * 1  +  i>=0 && 1  +  -1 * i>=0 && -1 * i>=0);
assume(i>=0);
i := i;
TO: 21003;

FROM: 1001;
assume(-1 * 1  +  i>=0 && 1  +  -1 * i>=0 && -1 * i>=0);
assume(i<0);
i := i;
TO: 21004;

FROM: 1002;
assume(-1 * 1  +  i>=0 && 1  +  -1 * i>=0 && -1 * i>=0);
i := i;
TO: 21002;

FROM: 1003;
assume(-1 * 1  +  i>=0 && 1  +  -1 * i>=0 && -1 * i>=0);
assume(i>=0);
i := i;
TO: 21003;

FROM: 1003;
assume(-1 * 1  +  i>=0 && 1  +  -1 * i>=0 && -1 * i>=0);
assume(i<0);
i := i;
TO: 21004;

FROM: 1004;
assume(-1 * 1  +  i>=0 && 1  +  -1 * i>=0 && -1 * i>=0);
i := i;
TO: 21004;

FROM: 1001;
assume(i>=0 && -1 * 1  +  i>=0 && 1  +  -1 * i>=0 && -1 * i>=0);
assume(i>=0);
i := i;
TO: 21002;

FROM: 1001;
assume(i>=0 && -1 * 1  +  i>=0 && 1  +  -1 * i>=0 && -1 * i>=0);
assume(i>=0);
i := i;
TO: 21003;

FROM: 1001;
assume(i>=0 && -1 * 1  +  i>=0 && 1  +  -1 * i>=0 && -1 * i>=0);
assume(i<0);
i := i;
TO: 21004;

FROM: 1002;
assume(i>=0 && -1 * 1  +  i>=0 && 1  +  -1 * i>=0 && -1 * i>=0);
i := i;
TO: 21002;

FROM: 1003;
assume(i>=0 && -1 * 1  +  i>=0 && 1  +  -1 * i>=0 && -1 * i>=0);
assume(i>=0);
i := i;
TO: 21003;

FROM: 1003;
assume(i>=0 && -1 * 1  +  i>=0 && 1  +  -1 * i>=0 && -1 * i>=0);
assume(i<0);
i := i;
TO: 21004;

FROM: 1004;
assume(i>=0 && -1 * 1  +  i>=0 && 1  +  -1 * i>=0 && -1 * i>=0);
i := i;
TO: 21004;

FROM: 1001;
assume(-1 * 1  +  i>=0 && i>=0 && -1 * i>=0);
assume(i>=0);
i := i;
TO: 21002;

FROM: 1001;
assume(-1 * 1  +  i>=0 && i>=0 && -1 * i>=0);
assume(i>=0);
i := i;
TO: 21003;

FROM: 1001;
assume(-1 * 1  +  i>=0 && i>=0 && -1 * i>=0);
assume(i<0);
i := i;
TO: 21004;

FROM: 1002;
assume(-1 * 1  +  i>=0 && i>=0 && -1 * i>=0);
i := i;
TO: 21002;

FROM: 1003;
assume(-1 * 1  +  i>=0 && i>=0 && -1 * i>=0);
assume(i>=0);
i := i;
TO: 21003;

FROM: 1003;
assume(-1 * 1  +  i>=0 && i>=0 && -1 * i>=0);
assume(i<0);
i := i;
TO: 21004;

FROM: 1004;
assume(-1 * 1  +  i>=0 && i>=0 && -1 * i>=0);
i := i;
TO: 21004;

FROM: 1001;
assume(i>=0 && -1 * 1  +  i>=0 && -1 * i>=0);
assume(i>=0);
i := i;
TO: 21002;

FROM: 1001;
assume(i>=0 && -1 * 1  +  i>=0 && -1 * i>=0);
assume(i>=0);
i := i;
TO: 21003;

FROM: 1001;
assume(i>=0 && -1 * 1  +  i>=0 && -1 * i>=0);
assume(i<0);
i := i;
TO: 21004;

FROM: 1002;
assume(i>=0 && -1 * 1  +  i>=0 && -1 * i>=0);
i := i;
TO: 21002;

FROM: 1003;
assume(i>=0 && -1 * 1  +  i>=0 && -1 * i>=0);
assume(i>=0);
i := i;
TO: 21003;

FROM: 1003;
assume(i>=0 && -1 * 1  +  i>=0 && -1 * i>=0);
assume(i<0);
i := i;
TO: 21004;

FROM: 1004;
assume(i>=0 && -1 * 1  +  i>=0 && -1 * i>=0);
i := i;
TO: 21004;

FROM: 1001;
assume(-1 * 1  +  i>=0 && -1 * i>=0 && i>=0);
assume(i>=0);
i := i;
TO: 21002;

FROM: 1001;
assume(-1 * 1  +  i>=0 && -1 * i>=0 && i>=0);
assume(i>=0);
i := i;
TO: 21003;

FROM: 1001;
assume(-1 * 1  +  i>=0 && -1 * i>=0 && i>=0);
assume(i<0);
i := i;
TO: 21004;

FROM: 1002;
assume(-1 * 1  +  i>=0 && -1 * i>=0 && i>=0);
i := i;
TO: 21002;

FROM: 1003;
assume(-1 * 1  +  i>=0 && -1 * i>=0 && i>=0);
assume(i>=0);
i := i;
TO: 21003;

FROM: 1003;
assume(-1 * 1  +  i>=0 && -1 * i>=0 && i>=0);
assume(i<0);
i := i;
TO: 21004;

FROM: 1004;
assume(-1 * 1  +  i>=0 && -1 * i>=0 && i>=0);
i := i;
TO: 21004;

FROM: 1001;
assume(i>=0 && -1 * i>=0);
assume(i>=0);
i := i;
TO: 21002;

FROM: 1001;
assume(i>=0 && -1 * i>=0);
assume(i>=0);
i := i;
TO: 21003;

FROM: 1001;
assume(i>=0 && -1 * i>=0);
assume(i<0);
i := i;
TO: 21004;

FROM: 1002;
assume(i>=0 && -1 * i>=0);
i := i;
TO: 21002;

FROM: 1003;
assume(i>=0 && -1 * i>=0);
assume(i>=0);
i := i;
TO: 21003;

FROM: 1003;
assume(i>=0 && -1 * i>=0);
assume(i<0);
i := i;
TO: 21004;

FROM: 1004;
assume(i>=0 && -1 * i>=0);
i := i;
TO: 21004;

FROM: 21002;
TO: 21005;

FROM: 21005;
TO: 21005;

FROM: 21003;
assume(i>=0);
TO: 21003;

FROM: 21003;
assume(i<0);
TO: 21004;

FROM: 21004;
TO: 21004;
