START: 1000;
BUCHI: {1000, 65000};
CUTPOINT: {65001, 1000, 65000, 1001};
PRE: {n>=-64 && n<=63 && b>=-64 && b<=63 && x>=-64 && x<=63 && y>=-64 && y<=63 };


FROM: 1000;
assume(-1 * b>=0 && b>=0 && x>=0 && y>=0 && n  +  -1 * y>=0);
assume(b>=-4);
b := 1;
x := x;
y := 1  +  y;
n := n;
TO: 1000;

FROM: 1000;
assume(-1 * b>=0 && b>=0 && x>=0 && y>=0 && n  +  -1 * y>=0);
assume(b<=-5);
assume(b<=0);
b := 1;
x := x;
y := 1  +  y;
n := n;
TO: 1001;

FROM: 1001;
assume(-1 * b>=0 && b>=0 && x>=0 && y>=0 && n  +  -1 * y>=0);
assume(b>=1);
b := 1;
x := x;
y := 1  +  y;
n := n;
TO: 1000;

FROM: 1001;
assume(-1 * b>=0 && b>=0 && x>=0 && y>=0 && n  +  -1 * y>=0);
assume(b<=0);
b := 1;
x := x;
y := 1  +  y;
n := n;
TO: 1001;

FROM: 1000;
assume(-1 * b>=0 && b>=0 && x>=0 && y>=0 && n  +  -1 * y>=0);
assume(b>=-4);
b := b;
x := x;
y := 1  +  y;
n := n;
TO: 1000;

FROM: 1000;
assume(-1 * b>=0 && b>=0 && x>=0 && y>=0 && n  +  -1 * y>=0);
assume(b<=-5);
assume(b<=0);
b := b;
x := x;
y := 1  +  y;
n := n;
TO: 1001;

FROM: 1001;
assume(-1 * b>=0 && b>=0 && x>=0 && y>=0 && n  +  -1 * y>=0);
assume(b>=1);
b := b;
x := x;
y := 1  +  y;
n := n;
TO: 1000;

FROM: 1001;
assume(-1 * b>=0 && b>=0 && x>=0 && y>=0 && n  +  -1 * y>=0);
assume(b<=0);
b := b;
x := x;
y := 1  +  y;
n := n;
TO: 1001;

FROM: 1000;
assume(-1 * 1  +  b>=0 && x>=0 && y>=0 && n  +  -1 * y>=0);
assume(b>=-4);
b := 0;
x := -1 * 1  +  x;
y := -1 * 1  +  y;
n := n;
TO: 1000;

FROM: 1000;
assume(-1 * 1  +  b>=0 && x>=0 && y>=0 && n  +  -1 * y>=0);
assume(b<=-5);
assume(b<=0);
b := 0;
x := -1 * 1  +  x;
y := -1 * 1  +  y;
n := n;
TO: 1001;

FROM: 1001;
assume(-1 * 1  +  b>=0 && x>=0 && y>=0 && n  +  -1 * y>=0);
assume(b>=1);
b := 0;
x := -1 * 1  +  x;
y := -1 * 1  +  y;
n := n;
TO: 1000;

FROM: 1001;
assume(-1 * 1  +  b>=0 && x>=0 && y>=0 && n  +  -1 * y>=0);
assume(b<=0);
b := 0;
x := -1 * 1  +  x;
y := -1 * 1  +  y;
n := n;
TO: 1001;

FROM: 1000;
assume(-1 * 1  +  b>=0 && x>=0 && y>=0 && n  +  -1 * y>=0);
assume(b>=-4);
b := b;
x := x;
y := -1 * 1  +  y;
n := n;
TO: 1000;

FROM: 1000;
assume(-1 * 1  +  b>=0 && x>=0 && y>=0 && n  +  -1 * y>=0);
assume(b<=-5);
assume(b<=0);
b := b;
x := x;
y := -1 * 1  +  y;
n := n;
TO: 1001;

FROM: 1001;
assume(-1 * 1  +  b>=0 && x>=0 && y>=0 && n  +  -1 * y>=0);
assume(b>=1);
b := b;
x := x;
y := -1 * 1  +  y;
n := n;
TO: 1000;

FROM: 1001;
assume(-1 * 1  +  b>=0 && x>=0 && y>=0 && n  +  -1 * y>=0);
assume(b<=0);
b := b;
x := x;
y := -1 * 1  +  y;
n := n;
TO: 1001;

FROM: 1000;
assume(-1 * 1  +  -1 * b>=0 && x>=0 && y>=0 && n  +  -1 * y>=0);
assume(b>=-4);
b := 0;
x := -1 * 1  +  x;
y := -1 * 1  +  y;
n := n;
TO: 1000;

FROM: 1000;
assume(-1 * 1  +  -1 * b>=0 && x>=0 && y>=0 && n  +  -1 * y>=0);
assume(b<=-5);
assume(b<=0);
b := 0;
x := -1 * 1  +  x;
y := -1 * 1  +  y;
n := n;
TO: 1001;

FROM: 1001;
assume(-1 * 1  +  -1 * b>=0 && x>=0 && y>=0 && n  +  -1 * y>=0);
assume(b>=1);
b := 0;
x := -1 * 1  +  x;
y := -1 * 1  +  y;
n := n;
TO: 1000;

FROM: 1001;
assume(-1 * 1  +  -1 * b>=0 && x>=0 && y>=0 && n  +  -1 * y>=0);
assume(b<=0);
b := 0;
x := -1 * 1  +  x;
y := -1 * 1  +  y;
n := n;
TO: 1001;

FROM: 1000;
assume(-1 * 1  +  -1 * b>=0 && x>=0 && y>=0 && n  +  -1 * y>=0);
assume(b>=-4);
b := b;
x := x;
y := -1 * 1  +  y;
n := n;
TO: 1000;

FROM: 1000;
assume(-1 * 1  +  -1 * b>=0 && x>=0 && y>=0 && n  +  -1 * y>=0);
assume(b<=-5);
assume(b<=0);
b := b;
x := x;
y := -1 * 1  +  y;
n := n;
TO: 1001;

FROM: 1001;
assume(-1 * 1  +  -1 * b>=0 && x>=0 && y>=0 && n  +  -1 * y>=0);
assume(b>=1);
b := b;
x := x;
y := -1 * 1  +  y;
n := n;
TO: 1000;

FROM: 1001;
assume(-1 * 1  +  -1 * b>=0 && x>=0 && y>=0 && n  +  -1 * y>=0);
assume(b<=0);
b := b;
x := x;
y := -1 * 1  +  y;
n := n;
TO: 1001;

FROM: 1000;
assume(-1 * 1  +  -1 * x>=0);
assume(b>=-4);
b := b;
x := x;
y := y;
n := n;
TO: 65000;

FROM: 1000;
assume(-1 * 1  +  -1 * x>=0);
assume(b<=-5);
assume(b<=0);
b := b;
x := x;
y := y;
n := n;
TO: 65001;

FROM: 1001;
assume(-1 * 1  +  -1 * x>=0);
assume(b>=1);
b := b;
x := x;
y := y;
n := n;
TO: 65000;

FROM: 1001;
assume(-1 * 1  +  -1 * x>=0);
assume(b<=0);
b := b;
x := x;
y := y;
n := n;
TO: 65001;

FROM: 1000;
assume(-1 * 1  +  -1 * y>=0);
assume(b>=-4);
b := b;
x := x;
y := y;
n := n;
TO: 65000;

FROM: 1000;
assume(-1 * 1  +  -1 * y>=0);
assume(b<=-5);
assume(b<=0);
b := b;
x := x;
y := y;
n := n;
TO: 65001;

FROM: 1001;
assume(-1 * 1  +  -1 * y>=0);
assume(b>=1);
b := b;
x := x;
y := y;
n := n;
TO: 65000;

FROM: 1001;
assume(-1 * 1  +  -1 * y>=0);
assume(b<=0);
b := b;
x := x;
y := y;
n := n;
TO: 65001;

FROM: 1000;
assume(-1 * 1  +  -1 * n  +  y>=0);
assume(b>=-4);
b := b;
x := x;
y := y;
n := n;
TO: 65000;

FROM: 1000;
assume(-1 * 1  +  -1 * n  +  y>=0);
assume(b<=-5);
assume(b<=0);
b := b;
x := x;
y := y;
n := n;
TO: 65001;

FROM: 1001;
assume(-1 * 1  +  -1 * n  +  y>=0);
assume(b>=1);
b := b;
x := x;
y := y;
n := n;
TO: 65000;

FROM: 1001;
assume(-1 * 1  +  -1 * n  +  y>=0);
assume(b<=0);
b := b;
x := x;
y := y;
n := n;
TO: 65001;

FROM: 65000;
assume(b>=-4);
TO: 65000;

FROM: 65000;
assume(b<=-5);
assume(b<=0);
TO: 65001;

FROM: 65001;
assume(b>=1);
TO: 65000;

FROM: 65001;
assume(b<=0);
TO: 65001;
