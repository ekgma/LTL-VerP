START: 1001;
BUCHI: {64002, 1002};
CUTPOINT: {64002, 64001, 1002, 1001};
PRE: {i>=-64 && i<=63 };


FROM: 1001;
assume(-3 * 1  +  -1 * i>=0 && -1 * 1  +  -1 * i>=0);
assume(i<0);
i := -2 * 1  +  -1 * i;
TO: 1001;

FROM: 1001;
assume(-3 * 1  +  -1 * i>=0 && -1 * 1  +  -1 * i>=0);
assume(i>=0);
i := -2 * 1  +  -1 * i;
TO: 1002;

FROM: 1002;
assume(-3 * 1  +  -1 * i>=0 && -1 * 1  +  -1 * i>=0);
assume(i<0);
i := -2 * 1  +  -1 * i;
TO: 1001;

FROM: 1002;
assume(-3 * 1  +  -1 * i>=0 && -1 * 1  +  -1 * i>=0);
assume(i>=0);
i := -2 * 1  +  -1 * i;
TO: 1002;

FROM: 1001;
assume(2 * 1  +  i>=0 && -1 * 1  +  -1 * i>=0);
assume(i<0);
i := 2 * 1  +  i;
TO: 1001;

FROM: 1001;
assume(2 * 1  +  i>=0 && -1 * 1  +  -1 * i>=0);
assume(i>=0);
i := 2 * 1  +  i;
TO: 1002;

FROM: 1002;
assume(2 * 1  +  i>=0 && -1 * 1  +  -1 * i>=0);
assume(i<0);
i := 2 * 1  +  i;
TO: 1001;

FROM: 1002;
assume(2 * 1  +  i>=0 && -1 * 1  +  -1 * i>=0);
assume(i>=0);
i := 2 * 1  +  i;
TO: 1002;

FROM: 1001;
assume(-3 * 1  +  i>=0 && i>=0 && -1 * 1  +  -1 * i>=0);
assume(i<0);
i := 2 * 1  +  -1 * i;
TO: 1001;

FROM: 1001;
assume(-3 * 1  +  i>=0 && i>=0 && -1 * 1  +  -1 * i>=0);
assume(i>=0);
i := 2 * 1  +  -1 * i;
TO: 1002;

FROM: 1002;
assume(-3 * 1  +  i>=0 && i>=0 && -1 * 1  +  -1 * i>=0);
assume(i<0);
i := 2 * 1  +  -1 * i;
TO: 1001;

FROM: 1002;
assume(-3 * 1  +  i>=0 && i>=0 && -1 * 1  +  -1 * i>=0);
assume(i>=0);
i := 2 * 1  +  -1 * i;
TO: 1002;

FROM: 1001;
assume(2 * 1  +  -1 * i>=0 && i>=0 && -1 * 1  +  -1 * i>=0);
assume(i<0);
i := -2 * 1  +  i;
TO: 1001;

FROM: 1001;
assume(2 * 1  +  -1 * i>=0 && i>=0 && -1 * 1  +  -1 * i>=0);
assume(i>=0);
i := -2 * 1  +  i;
TO: 1002;

FROM: 1002;
assume(2 * 1  +  -1 * i>=0 && i>=0 && -1 * 1  +  -1 * i>=0);
assume(i<0);
i := -2 * 1  +  i;
TO: 1001;

FROM: 1002;
assume(2 * 1  +  -1 * i>=0 && i>=0 && -1 * 1  +  -1 * i>=0);
assume(i>=0);
i := -2 * 1  +  i;
TO: 1002;

FROM: 1001;
assume(-3 * 1  +  -1 * i>=0 && -1 * 1  +  -1 * i>=0 && -1 * 1  +  i>=0);
assume(i<0);
i := -2 * 1  +  -1 * i;
TO: 1001;

FROM: 1001;
assume(-3 * 1  +  -1 * i>=0 && -1 * 1  +  -1 * i>=0 && -1 * 1  +  i>=0);
assume(i>=0);
i := -2 * 1  +  -1 * i;
TO: 1002;

FROM: 1002;
assume(-3 * 1  +  -1 * i>=0 && -1 * 1  +  -1 * i>=0 && -1 * 1  +  i>=0);
assume(i<0);
i := -2 * 1  +  -1 * i;
TO: 1001;

FROM: 1002;
assume(-3 * 1  +  -1 * i>=0 && -1 * 1  +  -1 * i>=0 && -1 * 1  +  i>=0);
assume(i>=0);
i := -2 * 1  +  -1 * i;
TO: 1002;

FROM: 1001;
assume(2 * 1  +  i>=0 && -1 * 1  +  -1 * i>=0 && -1 * 1  +  i>=0);
assume(i<0);
i := 2 * 1  +  i;
TO: 1001;

FROM: 1001;
assume(2 * 1  +  i>=0 && -1 * 1  +  -1 * i>=0 && -1 * 1  +  i>=0);
assume(i>=0);
i := 2 * 1  +  i;
TO: 1002;

FROM: 1002;
assume(2 * 1  +  i>=0 && -1 * 1  +  -1 * i>=0 && -1 * 1  +  i>=0);
assume(i<0);
i := 2 * 1  +  i;
TO: 1001;

FROM: 1002;
assume(2 * 1  +  i>=0 && -1 * 1  +  -1 * i>=0 && -1 * 1  +  i>=0);
assume(i>=0);
i := 2 * 1  +  i;
TO: 1002;

FROM: 1001;
assume(-3 * 1  +  i>=0 && i>=0 && -1 * 1  +  i>=0);
assume(i<0);
i := 2 * 1  +  -1 * i;
TO: 1001;

FROM: 1001;
assume(-3 * 1  +  i>=0 && i>=0 && -1 * 1  +  i>=0);
assume(i>=0);
i := 2 * 1  +  -1 * i;
TO: 1002;

FROM: 1002;
assume(-3 * 1  +  i>=0 && i>=0 && -1 * 1  +  i>=0);
assume(i<0);
i := 2 * 1  +  -1 * i;
TO: 1001;

FROM: 1002;
assume(-3 * 1  +  i>=0 && i>=0 && -1 * 1  +  i>=0);
assume(i>=0);
i := 2 * 1  +  -1 * i;
TO: 1002;

FROM: 1001;
assume(2 * 1  +  -1 * i>=0 && i>=0 && -1 * 1  +  i>=0);
assume(i<0);
i := -2 * 1  +  i;
TO: 1001;

FROM: 1001;
assume(2 * 1  +  -1 * i>=0 && i>=0 && -1 * 1  +  i>=0);
assume(i>=0);
i := -2 * 1  +  i;
TO: 1002;

FROM: 1002;
assume(2 * 1  +  -1 * i>=0 && i>=0 && -1 * 1  +  i>=0);
assume(i<0);
i := -2 * 1  +  i;
TO: 1001;

FROM: 1002;
assume(2 * 1  +  -1 * i>=0 && i>=0 && -1 * 1  +  i>=0);
assume(i>=0);
i := -2 * 1  +  i;
TO: 1002;

FROM: 1001;
assume(i>=0 && -1 * i>=0);
assume(i<0);
i := i;
TO: 64001;

FROM: 1001;
assume(i>=0 && -1 * i>=0);
assume(i>=0);
i := i;
TO: 64002;

FROM: 1002;
assume(i>=0 && -1 * i>=0);
assume(i<0);
i := i;
TO: 64001;

FROM: 1002;
assume(i>=0 && -1 * i>=0);
assume(i>=0);
i := i;
TO: 64002;

FROM: 64001;
assume(i<0);
TO: 64001;

FROM: 64001;
assume(i>=0);
TO: 64002;

FROM: 64002;
assume(i<0);
TO: 64001;

FROM: 64002;
assume(i>=0);
TO: 64002;
